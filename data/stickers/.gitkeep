Keeps this directory in the repository.

compose.yml bind-mounts ./data into the container. If the directory does not
exist on the host, Docker creates it as root - and the container runs as an
unprivileged user, so it then cannot write the sticker library into it. Shipping
the directory means the checkout owns it and the mount just works.
